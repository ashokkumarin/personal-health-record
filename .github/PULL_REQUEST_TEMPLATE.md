## What does this change?

<!-- Brief description of the change and why it's needed. Link the issue it resolves, if any. -->

## How was this tested?

<!-- e.g. `npm run test:api`, manual verification in web/mobile, screenshots for UI changes -->

## Checklist

- [ ] `npm run test:api` passes (if `apps/api` changed)
- [ ] Relevant docs (`docs/`, README) updated if behavior/setup changed
- [ ] No secrets or `.env` values committed
